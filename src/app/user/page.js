import React from "react";

const User = () => {
  return (
    <div>
      <h3>Pieash User</h3>
    </div>
  );
};

export default User;

export const generateMetadata = () => {
  return {
    title: "User data",
    description: "User page description",
  };
};
